# The program demonstrates the concept of fifo using two files client and server
# The client requests a file to be displayed
# The server returns the contents of the file if it exists
# You can also try running the client first and server next manually
./server & 
./client
